# Backend Modules Agent Guide

## Purpose

This folder groups the backend by business capability.

## Rules

- Each module owns its application, domain, infrastructure, and HTTP slices.
- Prefer local module logic over global abstractions until duplication is real.
