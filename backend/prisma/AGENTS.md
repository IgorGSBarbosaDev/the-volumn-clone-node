# Prisma Agent Guide

## Purpose

This folder owns the PostgreSQL schema, migrations, and seed data.

## Rules

- Keep persistence shape aligned with shared contracts and domain rules.
- Do not encode business policy solely in database structure.
